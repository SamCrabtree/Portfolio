const html = 5;
const css = 5;
const javaScript = 2;

var skills= {html, css, javaScript};

const itemButton = document.getElementById('itemButton');

mySubmit.addEventListener('click', function(skillMeter){
 
function skillMeter(){
for  (let i = 0; i < skills.length; i++){

   let value skills[i]

}

document.getElementById("display").innerHTML = 
<table>
    <tr>
        <th>Skill</th>
        <th>Proficiency</th>
    </tr>
    <tr>
        <td>$[skills[0]]</td>
        <td>$[value[0]]</td>
    </tr>
</table>
}

}
